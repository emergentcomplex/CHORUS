# Filename: infrastructure/debezium/Dockerfile.setup
# This Dockerfile creates a minimal utility image with all tools needed for setup.

FROM debian:bookworm-slim

# Install curl (to communicate with the Kafka Connect API) and the
# postgresql-client (to poll the database and wait for it to be ready).
# --no-install-recommends prevents installation of unnecessary packages.
# rm -rf /var/lib/apt/lists/* cleans up the apt cache to keep the image small.
RUN apt-get update && apt-get install -y --no-install-recommends curl postgresql-client && rm -rf /var/lib/apt/lists/*