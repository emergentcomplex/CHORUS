# Filename: infrastructure/postgres/postgresql.conf
# CHORUS Custom PostgreSQL Configuration

# THE DEFINITIVE FIX: Listen on all network interfaces to accept connections
# from other containers on the Docker network.
listen_addresses = '*'

# Enable logical replication for Debezium
wal_level = logical